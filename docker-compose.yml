services:
  postgres:
    image: postgres:17
    container_name: briefly-postgres
    environment:
      POSTGRES_USER: briefly
      POSTGRES_PASSWORD: briefly
      POSTGRES_DB: briefly_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
