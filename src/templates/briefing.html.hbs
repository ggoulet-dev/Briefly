<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Briefly — {{date}}</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; margin: 0; padding: 0; background: #f5f5f5; color: #333; }
    .container { max-width: 600px; margin: 0 auto; background: #fff; }
    .header { background: #1a1a2e; color: #fff; padding: 24px 32px; }
    .header h1 { margin: 0; font-size: 24px; font-weight: 600; }
    .header p { margin: 8px 0 0; opacity: 0.8; font-size: 14px; }
    .content { padding: 24px 32px; }
    .topic { margin-bottom: 28px; }
    .topic-title { font-size: 18px; font-weight: 600; color: #1a1a2e; border-bottom: 2px solid #e0e0e0; padding-bottom: 8px; margin-bottom: 16px; }
    .article { margin-bottom: 16px; padding-bottom: 16px; border-bottom: 1px solid #f0f0f0; }
    .article:last-child { border-bottom: none; padding-bottom: 0; }
    .article-title { font-size: 15px; font-weight: 600; margin: 0 0 6px; }
    .article-title a { color: #1a1a2e; text-decoration: none; }
    .article-title a:hover { text-decoration: underline; }
    .article-summary { font-size: 14px; line-height: 1.5; color: #555; margin: 0 0 6px; }
    .article-meta { font-size: 12px; color: #999; }
    .footer { padding: 20px 32px; background: #f9f9f9; text-align: center; font-size: 12px; color: #999; }
    .footer a { color: #666; }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Briefly</h1>
      <p>Hello {{userName}} — here's your briefing for {{date}}</p>
    </div>
    <div class="content">
      {{#each sections}}
      <div class="topic">
        <div class="topic-title">{{topicName}}</div>
        {{#each articles}}
        <div class="article">
          <div class="article-title"><a href="{{url}}">{{title}}</a></div>
          <div class="article-summary">{{summary}}</div>
          <div class="article-meta">{{sourceName}}{{#if author}} — {{author}}{{/if}}</div>
        </div>
        {{/each}}
      </div>
      {{/each}}
    </div>
    <div class="footer">
      <p>You received this because you're subscribed to Briefly.</p>
      <p><a href="{{unsubscribeUrl}}">Manage preferences</a></p>
    </div>
  </div>
</body>
</html>
